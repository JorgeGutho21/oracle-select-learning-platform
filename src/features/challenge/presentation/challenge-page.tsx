import { FeaturePlaceholder } from '@/presentation/components/feature-placeholder';

export function ChallengePage() {
  return (
    <FeaturePlaceholder
      title="SQL Oracle Challenge"
      description="El espacio de actividades de la unidad SELECT."
      emptyTitle="Las misiones están en preparación"
      emptyDescription="Todavía no hay actividades disponibles ni partidas iniciadas."
      section="Aplicar"
    />
  );
}
